1절. 변수
   할당과 동시에 타입설정
   문자,_,숫자만 사용가능. 문자로시작
   예약어 사용불가
   다중 변수 할당 가능(일괄 할당)
2절 화면 I/O
   input(label) : 사용자로부터 문자영 입력
   print(출력내용들,sep="구분자",end="종결어구)
3장 기본자료형
   숫자 : int/float/complex
   형 변환시 int( ), float( ) 함수 사용 가능 
   문자
   논리
