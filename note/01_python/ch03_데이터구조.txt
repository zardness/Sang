3장 데이터 구조 : 여러 데이터를 한 변수에 담는게 목적

1절.  리스트 : [타입에 상관없이 0개 이상의 데이터들] 
     
        1.1 리스트생성 : [ ],list(나열가능한 자료),(range(from,to,step))
        
        1.2 기본정보 조회
             -리스트 변수(lst)[인덱스]  = lst(idx]
             -min(lst), max(lst), sum(lst), len(lst),
             - sorted(lst,reverse=True) : 정열된 lst를 반환
             - lst.sort(reverse = True) : lst가 정열된 값으로 변경
        
        1.3. 요소 추가
             - append(value), extend([values]), insert(idx,value)

        1.4. 인덱싱과 인덱싱 찾기
              - ist[idx]
              - index (찾을 데이터) : 없는 데이터를 찾을경우 error
              - 찾을데이터 in lst
              -lst,count(찾을 데이터) : 찾을 데이터가 몇개인지 숫자 반환

        1.5 원하는 요소 추출
              -슬라이싱
              -리스트컴프리헨션(원하는 데이터 추출)

        1.6 요소 수정 : 인덱싱이나 슬라이싱을 이용 

        1.7 요소 삭제 : pop( ), pop(idx), del lst[idx], remove(데이터)

        1.8 복제 (깊은 복사): 복제할 변수 = 원본데이터.copy( )
     

2절 튜플(리스트와 유사하나 수정 불가)
       하나의 요소만 가질때는, 뒤에 포함 ex)tup = (1)
       ,가 없으면 스칼라 데이터로 인식

3절. 딕셔너리 : {키:값} 키와 값의 쌍
      keys() : 키목록/ values() : 값목록 / items() : 키-값 목록
      읽기 : 변수[키] 보다 변수.get(키, 없을경우값)를 사용하여 에러를 줄이자
      추가 / 수정 : 변수[키]=값 / 변수.update(추가 하거나 수정할 값들)
      삭제 : 변수.pop(키)/del변수(키) - 해당 키값이 없으면 에러
      해당키가 있는지 여부 : 키 in 변수

4절. 셋 : {중복을 허용하지않는 집합} 순서(index)가 없음
         추가 : add(스칼라데이터나 튜플), update(데이터구조)
         삭제 : remove(지울데이터) - 해당 데이터가 없으면 에러
         해당 데이터가 있는지 여부 : 데이터 in 변수

5절. enumerate
      인덱스나 가상의 순번을 추가한 나열가능한 객체: for 문 사용